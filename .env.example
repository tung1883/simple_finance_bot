TOKEN=BOT_TOKEN
PROXY_URL=OPEN_AI_PROXY

# Coach: live web prefetch when the LLM intent router sets needs_live_web (chat only). Disable search entirely:
# WEB_SEARCH_ENABLED=false
# WEB_SEARCH_MAX_RESULTS=5

# Coach: fetch one URL (article text extract). SSRF guard blocks private/loopback hosts.
# WEB_FETCH_ENABLED=true
# WEB_FETCH_TIMEOUT_SEC=15
# WEB_FETCH_MAX_BYTES=2000000
# WEB_FETCH_MAX_CHARS=12000
# WEB_FETCH_USER_AGENT=CashButlerFinanceBot/1.0

# Coach: deterministic auto-fetch when prefetched search returns only homepages / thin
# snippets and the user asks to summarize or analyze. Tighter budget than interactive fetch_url.
# WEB_AUTOFETCH_ENABLED=true
# WEB_AUTOFETCH_TIMEOUT_SEC=8
# WEB_AUTOFETCH_MAX_CHARS=6000
# WEB_AUTOFETCH_MIN_HOMEPAGES=3

# Optional — spreadsheet sync (either put google-service-account.json in this folder, or set path below)
# GOOGLE_SERVICE_ACCOUNT_FILE=C:/path/to/service-account.json

# Default: user creates a Sheet, shares SA as Editor, runs /linksheet in Telegram.
# Set true only if the service account has Drive quota (rare for new accounts):
# GOOGLE_SHEETS_AUTO_CREATE=true

# Sharing (optional — SA invites these Gmail addresses; may not work on all user-owned files):
# GOOGLE_SHEETS_SHARE_EMAILS=you@gmail.com
# GOOGLE_SHEETS_SHARE_ROLE=writer
# GOOGLE_SHEETS_PUBLIC_LINK=true
